{
  "name": "osint-breachshield",
  "version": "2.0.0",
  "private": true,
  "description": "AI-Powered OSINT Breach Detection & Blockchain Audit Platform",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "dev": "python run_servers.py",
    "test": "npm --prefix backend test && npm --prefix frontend test -- --watchAll=false",
    "install:all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix contracts install",
    "postinstall": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {}
}
