{
  "name": "osint-breachshield",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps/web-dashboard run build",
    "dev": "python run_servers.py",
    "test": "npm --prefix services/api-gateway test && npm --prefix apps/web-dashboard test -- --watchAll=false",
    "install:all": "npm --prefix services/api-gateway install && npm --prefix apps/web-dashboard install",
    "postinstall": "npm --prefix services/api-gateway install && npm --prefix apps/web-dashboard install"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "chart.js": "^4.5.0",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2",
    "react-chartjs-2": "^5.3.0",
    "react-simple-maps": "^3.0.0",
    "telegraf": "^4.16.3"
  }
}
