/**
 * @file timelineService.js
 * @description Breach timeline chronological parser service.
 */

const { parseBreachTimeline } = require('../analytics/timelineParser');

module.exports = {
  parseBreachTimeline
};
